# 05 – Exceptions

Goal: handle errors gracefully and raise clear exceptions when needed.

## Checklist
- Read the try/except/else/finally examples.
- Raise `ValueError` for invalid inputs in `exercises.py`.
- Use custom messages so failures are easy to debug.

## Commands
```sh
py examples.py
py exercises.py
```
