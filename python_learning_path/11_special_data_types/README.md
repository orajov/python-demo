# 11 – Special data types

Goal: use enums, dataclasses, and collections to express intent clearly.

## Checklist
- See `Enum` and `dataclass` usage in `examples.py`.
- Implement a lightweight record in `exercises.py`.
- Choose the right container from `collections` for the job.

## Commands
```sh
py examples.py
py exercises.py
```
